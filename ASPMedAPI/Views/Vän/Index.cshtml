@model IEnumerable<ASPMedAPI.Models.Classes.Vän>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Person1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Vän_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Vän_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Vän_ID })
        </td>
    </tr>
}

</table>
