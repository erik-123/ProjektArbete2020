@model IEnumerable<ASPMedAPI.Models.Classes.VänListItem>
@{
    ViewBag.Title = "FriendList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>FriendList</h2>

@if (!(Model.Count() == 0))
{
    <table class="table">
        <tr>
            <th>
                Förnamn
            </th>
            <th>
                Efternamn
            </th>
            <th></th>
            <th></th>
        </tr>
        
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item. Förnamn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Efternamn)
                </td>
                <td>
                    @Html.ActionLink("Delete", "RemoveFriend", "Vän", new { friendID = item.UserId }, new { })
                </td>
                <td>
                    @Html.ActionLink("View profile", "ShowProfile", "Profil", new { showID = item.UserId }, new { })
                </td>
            </tr>
        }

    </table>
}
else
{
   
    <h4>You have no friends in your friend list.</h4>
}

<p>
    @Html.ActionLink("Add friends", "Search", "Profils")
</p>

