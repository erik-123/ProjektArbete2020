@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

   
    <ul class="navbar">
        <li>
         @Html.Action("CountRequest", "Vän", "incomingList") 
            </li>
       <li> @Html.ActionLink("Notifikationer", "Vän", "incomingList", routeValues: null, htmlAttributes: new { id = "data" })</li>

        
        <li>
            @Html.ActionLink("Inställningar", "Index", "Manage")
        </li>
        <li>
            @Html.ActionLink("Inloggad som " + User.Identity.GetUserName(), "Details", "Profils", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li> </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Logga ut</a></li>
    </ul>
    }
}
else
{
<ul class="nav navbar-nav navbar-right">
    <li>@Html.ActionLink("Registrera", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
    <li>@Html.ActionLink("Logga in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
</ul>
}
